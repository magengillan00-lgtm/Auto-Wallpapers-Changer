<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Migen Wallpaper Pro</title>
    <style>
        :root {
            --accent-home: #00d2ff;
            --accent-lock: #a855f7;
            --accent-settings: #f1c40f;
            --bg: #0f172a;
            --card-bg: rgba(255, 255, 255, 0.05);
            --text-main: #ffffff;
            --text-dim: #94a3b8;
        }

        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: 'Segoe UI', Tahoma, sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        .app-container {
            width: 100%;
            max-width: 500px;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        /* إدارة الشاشات */
        .screen { display: none; flex-direction: column; gap: 20px; animation: fadeIn 0.3s ease; }
        .screen.active { display: flex; }

        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        h1 { text-align: center; font-size: 24px; margin: 10px 0 20px 0; }

        /* القائمة الرئيسية */
        .menu-list { display: flex; flex-direction: column; gap: 15px; margin-top: 20px; }
        .menu-item {
            background: var(--card-bg);
            padding: 25px 20px;
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.1);
            cursor: pointer;
            display: flex; justify-content: space-between; align-items: center;
            font-size: 18px; font-weight: bold; transition: 0.3s;
        }
        .menu-item:active { transform: scale(0.98); background: rgba(255,255,255,0.1); }

        /* منطقة المعاينة */
        .preview-box {
            width: 150px; height: 260px; background: #1e293b; margin: 0 auto;
            border: 5px solid #334155; border-radius: 25px; overflow: hidden;
            display: flex; align-items: center; justify-content: center; position: relative;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        .preview-box img { width: 100%; height: 100%; object-fit: cover; }
        .preview-box .placeholder { color: var(--text-dim); font-size: 12px; text-align: center; padding: 10px; }

        /* الإعدادات */
        .settings-card {
            background: var(--card-bg);
            padding: 20px;
            border-radius: 20px;
            display: flex; flex-direction: column; gap: 20px;
        }
        .setting-row { display: flex; justify-content: space-between; align-items: center; }
        select {
            background: #1e293b; color: white; border: 1px solid #334155;
            padding: 8px; border-radius: 10px; outline: none; font-size: 14px;
        }

        /* الأزرار */
        .btn {
            padding: 15px; border-radius: 15px; border: none; font-weight: bold;
            cursor: pointer; font-size: 16px; transition: 0.3s; text-align: center;
        }
        .btn-add-home { background: var(--accent-home); color: #000; }
        .btn-add-lock { background: var(--accent-lock); color: #fff; }
        .btn-save { background: var(--accent-settings); color: #000; margin-top: 10px; }
        .btn-back { background: transparent; color: var(--text-dim); border: 1px solid #334155; margin-top: 5px; }

        /* المعرض */
        .gallery {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px;
            margin-top: 10px; padding-bottom: 20px;
        }
        .gallery-item {
            aspect-ratio: 9/16; background: #1e293b; border-radius: 12px;
            overflow: hidden; position: relative; border: 1px solid rgba(255,255,255,0.05);
        }
        .gallery-item img { width: 100%; height: 100%; object-fit: cover; }
        .remove-btn {
            position: absolute; top: 4px; right: 4px;
            background: #ef4444; color: white; border: none;
            border-radius: 50%; width: 24px; height: 24px; cursor: pointer;
            font-weight: bold; display: flex; align-items: center; justify-content: center;
        }

        /* سويتش (Switch) للإعدادات */
        .switch {
            position: relative; display: inline-block; width: 40px; height: 22px;
        }
        .switch input { opacity: 0; width: 0; height: 0; }
        .slider {
            position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0;
            background-color: #334155; transition: .4s; border-radius: 34px;
        }
        .slider:before {
            position: absolute; content: ""; height: 16px; width: 16px; left: 3px; bottom: 3px;
            background-color: white; transition: .4s; border-radius: 50%;
        }
        input:checked + .slider { background-color: var(--accent-settings); }
        input:checked + .slider:before { transform: translateX(18px); }
    </style>
</head>
<body>

    <div class="app-container">
        
        <div id="mainMenu" class="screen active">
            <h1 style="color: var(--accent-home);">خلفيات مِيجن Pro</h1>
            <div class="menu-list">
                <div class="menu-item" onclick="showScreen('homePage')">
                    <span>🏠 الشاشة الرئيسية</span>
                    <span style="color: var(--accent-home)">➜</span>
                </div>
                <div class="menu-item" onclick="showScreen('lockPage')">
                    <span>🔒 شاشة القفل</span>
                    <span style="color: var(--accent-lock)">➜</span>
                </div>
                <div class="menu-item" onclick="showScreen('settingsPage')">
                    <span>⚙️ الإعدادات الذكية</span>
                    <span style="color: var(--accent-settings)">➜</span>
                </div>
            </div>
        </div>

        <div id="homePage" class="screen">
            <h1>صور الرئيسية</h1>
            <div class="preview-box">
                <img id="previewHome" src="" style="display:none">
                <div class="placeholder" id="labelHome">لم يتم اختيار صور للرئيسية بعد</div>
            </div>
            <button class="btn btn-add-home" onclick="openPicker('home')">+ إضافة صور جديدة</button>
            <div id="galleryHome" class="gallery"></div>
            <button class="btn btn-back" onclick="showScreen('mainMenu')">العودة للقائمة</button>
        </div>

        <div id="lockPage" class="screen">
            <h1>صور شاشة القفل</h1>
            <div class="preview-box">
                <img id="previewLock" src="" style="display:none">
                <div class="placeholder" id="labelLock">لم يتم اختيار صور للقفل بعد</div>
            </div>
            <button class="btn btn-add-lock" onclick="openPicker('lock')">+ إضافة صور جديدة</button>
            <div id="galleryLock" class="gallery"></div>
            <button class="btn btn-back" onclick="showScreen('mainMenu')">العودة للقائمة</button>
        </div>

        <div id="settingsPage" class="screen">
            <h1 style="color: var(--accent-settings)">إعدادات التبديل</h1>
            <div class="settings-card">
                <div class="setting-row">
                    <span>تغيير كل:</span>
                    <select id="timeSelect">
                        <option value="5">5 دقائق</option>
                        <option value="60">ساعة كاملة</option>
                        <option value="1440">يوم كامل</option>
                    </select>
                </div>
                <div class="setting-row">
                    <span>الترتيب:</span>
                    <select id="orderSelect">
                        <option value="seq">بالتسلسل</option>
                        <option value="rnd">عشوائي</option>
                    </select>
                </div>
                <div class="setting-row">
                    <span>التبديل عند فتح القفل:</span>
                    <label class="switch">
                        <input type="checkbox" id="onUnlock" checked>
                        <span class="slider"></span>
                    </label>
                </div>
            </div>
            <button class="btn btn-save" onclick="saveAndExit()">حفظ التغييرات</button>
            <button class="btn btn-back" onclick="showScreen('mainMenu')">إلغاء</button>
        </div>

        <input type="file" id="filePicker" hidden multiple accept="image/*">
    </div>

    <script>
        let storage = {
            home: [],
            lock: []
        };
        let currentMode = 'home';

        function showScreen(id) {
            document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            window.scrollTo(0, 0);
        }

        function openPicker(mode) {
            currentMode = mode;
            document.getElementById('filePicker').click();
        }

        document.getElementById('filePicker').addEventListener('change', function(e) {
            const files = Array.from(e.target.files);
            files.forEach(file => {
                const reader = new FileReader();
                reader.onload = (event) => {
                    storage[currentMode].push(event.target.result);
                    renderGallery(currentMode);
                    updatePreview(currentMode, event.target.result);
                };
                reader.readAsDataURL(file);
            });
            this.value = ''; // إعادة التعيين لاختيار نفس الملف مجدداً إن لزم
        });

        function renderGallery(mode) {
            const galleryDiv = document.getElementById(mode === 'home' ? 'galleryHome' : 'galleryLock');
            galleryDiv.innerHTML = '';
            storage[mode].forEach((src, index) => {
                const item = document.createElement('div');
                item.className = 'gallery-item';
                item.innerHTML = `
                    <img src="${src}" onclick="updatePreview('${mode}', '${src}')">
                    <button class="remove-btn" onclick="deleteImg('${mode}', ${index})">×</button>
                `;
                galleryDiv.appendChild(item);
            });
        }

        function updatePreview(mode, src) {
            const imgTag = document.getElementById(mode === 'home' ? 'previewHome' : 'previewLock');
            const label = document.getElementById(mode === 'home' ? 'labelHome' : 'labelLock');
            imgTag.src = src;
            imgTag.style.display = 'block';
            label.style.display = 'none';
        }

        function deleteImg(mode, index) {
            storage[mode].splice(index, 1);
            renderGallery(mode);
            if (storage[mode].length === 0) {
                document.getElementById(mode === 'home' ? 'previewHome' : 'previewLock').style.display = 'none';
                document.getElementById(mode === 'home' ? 'labelHome' : 'labelLock').style.display = 'block';
            }
        }

        function saveAndExit() {
            // هنا سيتم مستقبلاً ربط الإعدادات بـ LocalStorage أو Cordova Plugin
            alert("تم حفظ تفضيلاتك يا مِيجن! التطبيق جاهز للعمل.");
            showScreen('mainMenu');
        }
    </script>
</body>
</html>
